apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fleetman-queue.fullname" . }}-test-connection"
  labels:
    {{- include "fleetman-queue.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fleetman-queue.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
