apiVersion: v1
kind: Service
metadata:
  name: {{ include "fleetman-queue.fullname" . }}
  labels:
    {{- include "fleetman-queue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fleetman-queue.selectorLabels" . | nindent 4 }}
