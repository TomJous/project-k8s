apiVersion: v1
kind: Service
metadata:
  name: {{ include "fleetman-position-simulator.fullname" . }}
  labels:
    {{- include "fleetman-position-simulator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fleetman-position-simulator.selectorLabels" . | nindent 4 }}
